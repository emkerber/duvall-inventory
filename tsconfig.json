{
    "compilerOptions": {
        // "outDir": "./build/",
        "strictNullChecks": true,
        "module": "preserve", // supports both es6 and node, so both imports and requires 
        "jsx": "preserve",
        "target": "es6",
        "allowJs": true
    },
    "include": [
        "./server/",
        "./src/"
    ]
}